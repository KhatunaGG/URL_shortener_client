export type UrlType = {
  url: string;
  shortCode: string;
  createdAt: string;
  updatedAt: string;
  accessCount: number;
  _id: string;
};

export type NewUrlType = {
  url: string;
  shortCode: string;
  createdAt: string;
  updatedAt: string;
  accessCount: number;
};
